Must install ballet/requirements.txt

Must have maven

first make build of Choco using the Makefile

examples to be run are in examples/
raw results in csv format are in results/ ; a synthesis can be made running analysis.py

